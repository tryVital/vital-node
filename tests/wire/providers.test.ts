// This file was auto-generated by Fern from our API Definition.

import * as Vital from "../../src/api/index";
import { VitalClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ProvidersClient", () => {
    test("get_all (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new VitalClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = [
            {
                name: "Oura",
                slug: "oura",
                description: "description",
                logo: "https://logo_url.com",
                auth_type: "oauth",
                supported_resources: ["workouts", "sleep"],
            },
        ];
        server.mockEndpoint().get("/v2/providers").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.providers.getAll({
            sourceType: "source_type",
        });
        expect(response).toEqual([
            {
                name: "Oura",
                slug: "oura",
                description: "description",
                logo: "https://logo_url.com",
                authType: "oauth",
                supportedResources: ["workouts", "sleep"],
            },
        ]);
    });

    test("get_all (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new VitalClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};
        server.mockEndpoint().get("/v2/providers").respondWith().statusCode(422).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.providers.getAll();
        }).rejects.toThrow(Vital.UnprocessableEntityError);
    });
});
