// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../BaseClient.js";
import { mergeHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import * as environments from "../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import * as serializers from "../../../../serialization/index.js";
import * as Vital from "../../../index.js";

export declare namespace IntrospectClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class IntrospectClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<IntrospectClient.Options>;

    constructor(options: IntrospectClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * @param {Vital.IntrospectGetUserResourcesRequest} request
     * @param {IntrospectClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Vital.UnprocessableEntityError}
     *
     * @example
     *     await client.introspect.getUserResources({
     *         userId: "user_id",
     *         provider: "oura",
     *         userLimit: 1,
     *         cursor: "cursor",
     *         nextCursor: "next_cursor"
     *     })
     */
    public getUserResources(
        request: Vital.IntrospectGetUserResourcesRequest = {},
        requestOptions?: IntrospectClient.RequestOptions,
    ): core.HttpResponsePromise<Vital.UserResourcesResponse> {
        return core.HttpResponsePromise.fromPromise(this.__getUserResources(request, requestOptions));
    }

    private async __getUserResources(
        request: Vital.IntrospectGetUserResourcesRequest = {},
        requestOptions?: IntrospectClient.RequestOptions,
    ): Promise<core.WithRawResponse<Vital.UserResourcesResponse>> {
        const { userId, provider, userLimit, cursor, nextCursor } = request;
        const _queryParams: Record<string, unknown> = {};
        if (userId != null) {
            _queryParams.user_id = userId;
        }

        if (provider != null) {
            _queryParams.provider = serializers.Providers.jsonOrThrow(provider, { unrecognizedObjectKeys: "strip" });
        }

        if (userLimit != null) {
            _queryParams.user_limit = userLimit;
        }

        if (cursor != null) {
            _queryParams.cursor = cursor;
        }

        if (nextCursor != null) {
            _queryParams.next_cursor = nextCursor;
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.VitalEnvironment.Production,
                "v2/introspect/resources",
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.UserResourcesResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Vital.UnprocessableEntityError(
                        serializers.HttpValidationError.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.VitalError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/v2/introspect/resources");
    }

    /**
     * @param {Vital.IntrospectGetUserHistoricalPullsRequest} request
     * @param {IntrospectClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Vital.UnprocessableEntityError}
     *
     * @example
     *     await client.introspect.getUserHistoricalPulls({
     *         userId: "user_id",
     *         provider: "oura",
     *         userLimit: 1,
     *         cursor: "cursor",
     *         nextCursor: "next_cursor"
     *     })
     */
    public getUserHistoricalPulls(
        request: Vital.IntrospectGetUserHistoricalPullsRequest = {},
        requestOptions?: IntrospectClient.RequestOptions,
    ): core.HttpResponsePromise<Vital.UserHistoricalPullsResponse> {
        return core.HttpResponsePromise.fromPromise(this.__getUserHistoricalPulls(request, requestOptions));
    }

    private async __getUserHistoricalPulls(
        request: Vital.IntrospectGetUserHistoricalPullsRequest = {},
        requestOptions?: IntrospectClient.RequestOptions,
    ): Promise<core.WithRawResponse<Vital.UserHistoricalPullsResponse>> {
        const { userId, provider, userLimit, cursor, nextCursor } = request;
        const _queryParams: Record<string, unknown> = {};
        if (userId != null) {
            _queryParams.user_id = userId;
        }

        if (provider != null) {
            _queryParams.provider = serializers.Providers.jsonOrThrow(provider, { unrecognizedObjectKeys: "strip" });
        }

        if (userLimit != null) {
            _queryParams.user_limit = userLimit;
        }

        if (cursor != null) {
            _queryParams.cursor = cursor;
        }

        if (nextCursor != null) {
            _queryParams.next_cursor = nextCursor;
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.VitalEnvironment.Production,
                "v2/introspect/historical_pull",
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.UserHistoricalPullsResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Vital.UnprocessableEntityError(
                        serializers.HttpValidationError.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.VitalError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/v2/introspect/historical_pull",
        );
    }
}
