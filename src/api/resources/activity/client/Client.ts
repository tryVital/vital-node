// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../BaseClient.js";
import { mergeHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import * as environments from "../../../../environments.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import * as serializers from "../../../../serialization/index.js";
import * as Vital from "../../../index.js";

export declare namespace ActivityClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class ActivityClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<ActivityClient.Options>;

    constructor(options: ActivityClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * Get activity summary for user_id
     *
     * @param {string} user_id
     * @param {Vital.ActivityGetRequest} request
     * @param {ActivityClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Vital.UnprocessableEntityError}
     *
     * @example
     *     await client.activity.get("user_id", {
     *         provider: "provider",
     *         startDate: "start_date",
     *         endDate: "end_date"
     *     })
     */
    public get(
        user_id: string,
        request: Vital.ActivityGetRequest,
        requestOptions?: ActivityClient.RequestOptions,
    ): core.HttpResponsePromise<Vital.ClientActivityResponse> {
        return core.HttpResponsePromise.fromPromise(this.__get(user_id, request, requestOptions));
    }

    private async __get(
        user_id: string,
        request: Vital.ActivityGetRequest,
        requestOptions?: ActivityClient.RequestOptions,
    ): Promise<core.WithRawResponse<Vital.ClientActivityResponse>> {
        const { provider, startDate, endDate } = request;
        const _queryParams: Record<string, unknown> = {};
        if (provider != null) {
            _queryParams.provider = provider;
        }

        _queryParams.start_date = startDate;
        if (endDate != null) {
            _queryParams.end_date = endDate;
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.VitalEnvironment.Production,
                `v2/summary/activity/${core.url.encodePathParam(user_id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.ClientActivityResponse.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Vital.UnprocessableEntityError(
                        serializers.HttpValidationError.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.VitalError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/v2/summary/activity/{user_id}",
        );
    }

    /**
     * Get raw activity summary for user_id
     *
     * @param {string} user_id
     * @param {Vital.ActivityGetRawRequest} request
     * @param {ActivityClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Vital.UnprocessableEntityError}
     *
     * @example
     *     await client.activity.getRaw("user_id", {
     *         provider: "provider",
     *         startDate: "start_date",
     *         endDate: "end_date"
     *     })
     */
    public getRaw(
        user_id: string,
        request: Vital.ActivityGetRawRequest,
        requestOptions?: ActivityClient.RequestOptions,
    ): core.HttpResponsePromise<Vital.RawActivity> {
        return core.HttpResponsePromise.fromPromise(this.__getRaw(user_id, request, requestOptions));
    }

    private async __getRaw(
        user_id: string,
        request: Vital.ActivityGetRawRequest,
        requestOptions?: ActivityClient.RequestOptions,
    ): Promise<core.WithRawResponse<Vital.RawActivity>> {
        const { provider, startDate, endDate } = request;
        const _queryParams: Record<string, unknown> = {};
        if (provider != null) {
            _queryParams.provider = provider;
        }

        _queryParams.start_date = startDate;
        if (endDate != null) {
            _queryParams.end_date = endDate;
        }

        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.VitalEnvironment.Production,
                `v2/summary/activity/${core.url.encodePathParam(user_id)}/raw`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: serializers.RawActivity.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                }),
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 422:
                    throw new Vital.UnprocessableEntityError(
                        serializers.HttpValidationError.parseOrThrow(_response.error.body, {
                            unrecognizedObjectKeys: "passthrough",
                            allowUnrecognizedUnionMembers: true,
                            allowUnrecognizedEnumValues: true,
                            breadcrumbsPrefix: ["response"],
                        }),
                        _response.rawResponse,
                    );
                default:
                    throw new errors.VitalError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/v2/summary/activity/{user_id}/raw",
        );
    }
}
